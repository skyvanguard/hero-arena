# Roadmap

**Status:** Phase 0 plan (compiled 2026-09-02)
**Method:** vertical slices (directive §37) — every phase ends with something *playable or runnable on the target hardware*, never months of isolated system work. Phase 0 (this) delivers research + architecture only.

---

## 0. Phase 0 — Research & Architecture (CURRENT)

**Deliverables:**
- [x] docs/T3_RESEARCH.md — T3 Arena mechanics, roster, modes, monetization, community diagnosis
- [x] docs/OVERWATCH_RESEARCH.md — roles/sub-roles, perks, matchmaking, progression, reception
- [x] docs/OPEN_SOURCE_RESEARCH.md — 17+ projects evaluated (license, engine, activity, patterns)
- [x] docs/ENGINE_DECISION.md — Godot 4.7.x selected (vs Unity 6) with risk register
- [x] docs/ARCHITECTURE.md — system overview, module map, data model, decisions log
- [x] docs/ROADMAP.md — this file
- [x] Technical recommendation presented (Godot 4, authoritative headless server, data-driven content, bots as server controllers)

**Exit criteria:** docs reviewed; engine + license decided; repo initialized (code Apache-2.0, assets CC0/CC-BY per file).

## 1. Phase 1 — Core Prototype (vertical slice)

**Goal: the smallest thing that proves the game is FUN on Android** (directive §37 checklist, verbatim):

- [x] Android build pipeline (Godot 4.7.2 headless export → signed debug APK, SDK/Java config, install verified on Android 34 emulator — `export_presets.cfg`, `docs/PERFORMANCE.md`; real-device install pending with the perf note)
- [x] 1 small 3D arena map (placeholder-quality, correct *scale* — lanes/cover/verticality) — `game/gameplay/modes/arena.gd`, headless-verified
- [x] Third-person character (capsule, movement states, coyote/buffer) — `core/state/character_entity.gd`, tested
- [x] TPS camera (drag-look, shoulder offset; fixed-offset instead of SpringArm — 4.7 rework) — `gameplay/character/hero.gd`, tested
- [x] Mobile touch controls (left move stick, right aim stick, fire, jump, reload) — `core/input/touch_controls.gd`; on-device check pending (line 36)
- [x] 1 weapon (hitscan, ammo, reload, headshot mult) — `core/combat/weapon.gd`, tested (sound pending asset pipeline)
- [x] Damage + health (HP bar, hit flash, damage numbers, regen-to-cap) — tested
- [x] Death + respawn (6 s world-clock timer, 2 s protection) — `core/state/world.gd`, tested
- [x] 1 enemy bot (search/engage/retreat, 4 difficulty param packs as data) — `gameplay/bots/bot_controller.gd`, tested
- [x] Desktop KB/mouse binding of the same input contract (dev convenience) — `core/input/desktop_input.gd`
- [x] Headless sim test harness + first gameplay tests (movement, hit, death, reload, bot) — `tests/test_main.gd`, 16/16 green
- [ ] Real-device perf note: FPS on a low-end + mid-range Android phone, memory, thermals (PERFORMANCE.md baseline)

**Exit criteria:** a person can install the APK, pick the character, run/shoot/kill the bot, die, respawn, on a real phone at ≥30 FPS. All in one 5-minute session. This is the go/no-go gate for the engine decision (D1 risk: if Godot misses the floor, revisit before Phase 2).

## 2. Phase 2 — First Hero

- [x] Original hero #1 — KESTREL (identity doc in GAME_DESIGN.md; kit live in `content/heroes/kestrel.tres`)
- [x] Hero select screen (data-driven `HeroRegistry`; roster cards + DEPLOY, touch/Enter — verified on emulator)
- [x] Data-driven hero registry (`content/heroes/registry.gd`) — adding a hero = one .tres + one line
- [x] Ability framework v1 (data-driven .tres definitions; cast/cooldown/charge) — `content/heroes/*_data.gd`, `gameplay/hero/ability_component.gd`
- [x] Ultimate with combat-driven charge (dmg dealt/taken + kills; HUD ult bar) — tested
- [x] Placeholder → original VFX + SFX for the hero's full kit — 9 synthesized original SFX (`tools/gen_sfx.gd`), kit VFX (dash/burst/ult aura), muzzle flash + gun recoil; pipeline docs in `docs/ASSET_PIPELINE.md` + `assets/LICENSE-ASSETS.md`
- [x] Animation v1 (procedural cosmetics: recoil/flash/movement-driven VFX)
- [ ] Skeletal animation set (idle/run/jump/shoot/ability/death — original, with Phase 3 models)
- [x] Hero tests headless (kit, registry, recoil, cooldowns, charge math, ability effects) — `tests/test_hero.gd`, 40/40 green

**Exit criteria:** hero #1 is *a character* — you can understand them in seconds and feel the kit in minutes. No other hero needed yet (the framework proves out on one).

## 3. Phase 3 — Hero Framework + Roster to 6

- [x] Role/sub-role passive registry (data-only: HIT_STREAK / SPRINT / ARMOR in `content/heroes/passive_data.gd`; new kind = data + one match arm) — SPRINT/ARMOR live on Blitz/Bastion; FIELD/FLEX/ZONE with heroes #4-6
- [x] Weapon framework v1 (hitscan w/ per-pellet spread + stepped projectile entities + headshot multipliers) — `core/combat/weapon.gd` (mode/pellets), `core/combat/projectile.gd`; heat/charge tables with heroes #4-6
- [x] Heroes #2–#6 (2 Assault, 1 Tank, 2 Support/Utility, 1 Controller per directive §6) — each with distinct movement/combat identity. All 6 built & tested (Blitz/Bastion/Mira/Patch/Nimbus in Phase 3 rounds 3–4); identities in GAME_DESIGN.md
- [x] Practice range (hero testing, dummy targets, timer) — offline, no cloud: `gameplay/practice/` (range builder + server-side manager), `ui/practice_hud.gd`, PRACTICE RANGE button on the select screen, 11-check headless suite (tests/test_practice.tscn) incl. the real player camera-aim path; emulator-verified end to end
- [x] Balance config system (content/balance/) — `Balance` (role bands + per-hero entry index) + `BalanceEntry` per-hero tuning multipliers (hp/damage/fire-rate/speed/ult-charge), applied server-side by HeroFactory at spawn; the headless roster test generates the DPS/TTK table from this config and enforces the config bands (baseline pass: all multipliers 1.0)
- [x] Roster tests: per-hero kit validation + DPS/TTK balance table with **config-driven role bands** (`tests/test_roster.tscn`, **all 6 heroes green** incl. support heal/boost + controller zone checks). Also enforces: one balance entry per hero + ult-charge pacing ≤ 12 kills worst-case. CC-lock: tracked (CC budget = Bastion Quake Stomp + Nimbus Ion slow, both slow-only — no locks).

**Exit criteria:** 6 heroes selectable, each feels like a different game in ≤5 minutes; practice range works fully offline; balance table generated by test.

**Status: MET (round 6).** All 6 heroes selectable from the select screen (emulator-verified, each with a distinct movement/combat identity in GAME_DESIGN.md); practice range runs fully offline (no cloud/bots, 11-check suite + device pass); the DPS/TTK balance table is generated by `tests/test_roster.tscn` from `content/balance/`. All 5 headless suites green; shipping (mobile-renderer) APK rebuilt and smoke-tested.

## 4. Phase 4 — Bot System

- [x] Perception module (vision cones + LOS raycast, hearing from shot events, ally state, per-tick scan with memory; all gated by the difficulty pack — vision range/FOV/hearing/lost-sight timeout)
- [x] Decision module (intent ladder v1: RETREAT → ATTACK → INVESTIGATE → REGROUP → HOLD with role responsibilities: tank closes + holds, support regroups harder + fights at range, retreat-confirmation window + re-engage hysteresis so bots are killable)
- [x] Execution module (strafing, approach/hold fight range, aim with **angular** error model resampled at 5 Hz, auto-reload, ability timing gated by difficulty `ability_quality`, scan-while-holding; direct goal pursuit — navmesh pathing + cover seeking arrive with the Phase 6 enclosed maps)
- [ ] Team behavior — partial: regroup + retreat + shot callouts (hearing) + **flank spread** (shared-target lateral offsets, `flank_spacing`) + **stick/protect** (idle bots drift back to in-combat allies, `stick_range`) in; pathed flanking, protect-from-behind, revive rules (needs downed state) pending with Phase 6 modes
- [x] 4 difficulty tiers (Beginner→Expert, parameter packs in `content/balance/bots/*.tres`; objective awareness N/A until Phase 6 modes)
- [x] Bot eval harness in CI (`tests/test_bots.tscn`, 13 checks: tier ordering, vision in/out-of-cone/LOS-blocked, hearing + position, decisions, navigation closure, **3v3 match: kills happen**, **expert squad out-K/Ds beginner squad**) — 13/13 green
- [x] Offline bot match (3v3 vs bots, difficulty selectable on the hero-select screen via `MatchConfig` autoload) — the "zero humans" guarantee (directive §2); emulator-verified: score/kill feed/respawn counting all live in a 3v3 with 5 bots + idle player

**Exit criteria:** a player can play a full offline 3v3 (self + 2 bots vs 3 bots) in TDM and Control that feels *played by someone*, with Beginner bots actually teachable and Expert bots threatening.

**Status: IN PROGRESS (round 8).** Bot system v1 + team behaviors v1 shipped: perception/decision/execution + 4 difficulty packs + offline 3v3 with a UI difficulty picker + **flank spread & stick/protect** (eval-verified); **TDM match end** (15-kill cap / 5:00 clock, `match_over` event, sim freeze, results overlay → tap back to select) makes offline 3v3 a *full match* per directive §6; 23/23 eval checks green incl. live 3v3 kill tests, team-behavior checks, and match-end checks; emulator-verified: full matches ran flat at ~188 MB with timer, kill feed, results overlay, and tap-to-return all working. (Round 7 baseline: 13/13 checks, 3v3 OOM fixed.) Two core bugs found by the bot eval and fixed along the way: (1) the head sensor was an `Area3D` — Godot 4.7 `intersect_ray` does not report Areas, so **heads were unhittable and headshots never registered** (now a `StaticBody3D`, detected via `CharacterEntity.hit_is_head`); (2) HUD canvas writes on the GL renderer (per-frame label/ColorRect writes + per-kill Label create/free) fragmented the 2D canvas texture pool and OOM'd a 3v3 in ~20 s (3.2 GB) on the emulator — now write-on-change + a fixed 6-label feed pool (see PERFORMANCE.md). Remaining before exit: flank/protect/revive team behaviors, navmesh pathing + cover seeking (Phase 6 maps), per-mode objective awareness, and the feel tuning pass (Beginner teachable / Expert threatening).

## 5. Phase 5 — Multiplayer

- [x] Dedicated headless match server (same codebase; 60 Hz fixed step) — net/server.tscn headless, --port=N, bot pre-fill; input sanitization + seq gate + session tokens (v1.1)
- [x] Net layer v1: **shipped (round 9): 20 Hz snapshots (unreliable) + reliable events + 100 ms interpolation + own wire codec** (docs/NETWORKING.md); **v1.1 (round 10): client prediction + reconciliation (hard-snap > 0.35 m) + server lag-comp hitscan (200 ms analytic rewind window)**
- [x] Reconnect (drop/resume mid-match) — server slot freeze + token reattach (same CharacterEntity), client bounded retry (6x0.5 s); net-sim verified (test_net_sim drop, re-hello, same instance)
- [x] **Match lifecycle after over (round 28)** — `World.reset()` + `MatchServer.reset_match()`: a fresh join into a finished match with **no humans left connected** starts a new match IN PLACE (fresh score/time/over, frozen tokens invalidated, bots re-filled via the `match_reset` signal — same process, same ports, discovery/lobby state flips back to OPEN); joins while other humans are still connected are rejected with the over code (v1: no mid-observation reset). Sim suite 8/8 (test_match_lifecycle) + live ENet verification (bot match over at t=55.9 -> single joiner reset it in place: slot + 19.9 Hz snapshot stream, seq-confirmed, discovery OVER->OPEN)
- [x] Server-authority property tests (client lies rejected: damage, ammo, cooldown, movement) — tests/test_net_props.gd, 20 checks (move/aim clamps, u16 wrap-aware seq gate + stale-edge rejection, ammo floor, shot-event parity, lag-comp geometry, server-side reattach)
- [x] Net-sim harness: **SimLink shipped (round 10)** — per-direction latency/loss + drop_all; **latency/loss profiles shipped (round 28)**: test_net_sim (150 ms RTT + 2% loss, 10 checks) + test_net_profiles (50 ms RTT + 10% / 150 ms + 2% / 300 ms RTT + 10%, same battery, 30 checks green) — connect/slot, input roundtrip, prediction tracking (RTT-scaled budget), 50 s combat + kill feed, drop → freeze → token reattach at every profile
- [x] **LAN play first** per directive §21 — **direct connect shipped** (host:port field in hero-select, emulator-verified join) + **LAN discovery shipped (round 11)**: UDP broadcast/unicast ping on a dedicated discovery port (7778), live open/full/over state, SCAN button in hero-select with tap-to-fill result rows; **two-device LAN verified (round 12)** on two Android emulators + a host-dedicated server (both devices SCAN-joined the same live match); **broadcast leg verified (round 28)** on a real host NIC: a broadcast-only scanner (no unicast) on a /24 LAN found a host-net docker server via 255.255.255.255:7778 and an ENet client joined through the LAN IP (slot + 20 Hz snapshot stream, server seq-confirmed); two real phones on real WiFi = final sign-off when devices are available
- [x] Internet play v1 (round 13: UDP lobby sidecar, region table + widen order, live ping + region cycle in the hero-select ONLINE panel, published-server addressing; NAT traversal/relay shipped round 29, item below)
- [x] **NAT traversal v1: relay (round 29, D15)** — servers behind a NAT register in the relay over an OUTBOUND UDP connection (control 7800, R_REG carries the game port) and get a VIRTUAL PORT (7901..8156); clients make a normal ENet connection to `<relay_ip>:<vport>` — the relay is a headerless per-(match,client) datagram pump (one ephemeral socket per link: distinct source ports to the server, replies back through the vport so the dialed address holds), ENet reliability/flow control intact. R_PING keep-alive 10 s; eviction 60 s match / 120 s link. **Zero client-protocol change**: the lobby just advertises the relay vport (server `--relay=ip:port` defers lobby registration until the vport is granted). Verified: test_relay 7/7 (two relayed matches in parallel: distinct vports, slots, 10 s ~20 Hz pacing per client, no cross-talk, dropped client re-joins its match through the relay) + **live NAT test** (server in a docker bridge container → host relay → probe client through the vport: slot + 199 snapshots/10 s + 207 events) + full flow lobby→assign(vport)→join in ~300 ms. Docker image: ENTRYPOINT+CMD convention (run args now work) + relay scene from the same image. Follow-ups: hole-punch for direct-capable NATs, relay HA, R_REG auth token, STUN-style auto-publish
- [x] Matchmaking prototype v1 (round 13: 4-stage party-aware queue strict → skill(no-op v1) → region widen LATAM-first → botfill@60 s; 12-check suite; transparent queue m:ss/stage in UI; party group-join handshake + match-found panel polish deferred)
- [x] Server ops: **docker image (round 27)** — `server/Dockerfile` (pinned Godot 4.7.2 binary + project + baked import cache, ~119 MB compressed) + **2-core budget measured (PERFORMANCE.md)**: full 3v3 bot match at ~0.19 cores avg, 60 Hz step held at world-time ratio 1.0000, 20 Hz snapshot pacing verified through published ports

**Exit criteria:** two phones on the same LAN play 3v3 with bots filling; then over the internet with bot fill at 60 s; drops/reconnects survive; property tests green.

**Status: IN PROGRESS (round 29).** **NAT traversal v1 shipped (this round):** the relay (D15) closes the last Phase 5 connectivity item runnable on this machine — servers behind a NAT get a relay virtual port, clients (unchanged protocol) join through it, and the lobby publishes the vport as the match address; verified by the 7-check suite AND a real network boundary (docker bridge container ↔ host relay ↔ client), plus the full lobby→assign→join flow (see NETWORKING §9.5). The real-WiFi two-phone sign-off and relay follow-ups (hole-punch, HA) remain. **Round 28 baseline (below) stands.** **Latency/loss profiles shipped (round 28):** the net-sim profile matrix is complete — test_net_profiles runs the full 10-check battery (connect/slot, input roundtrip, prediction tracking, 50 s combat + kill feed, drop → freeze → token reattach, server keeps stepping) at 50 ms RTT + 10% loss, the 150 ms RTT + 2% baseline, and 300 ms RTT + 10% loss: 30/30 green, clean exit, two consecutive green runs (loss is stochastic). The prediction budget scales with RTT (RTT/2 + interpolation unreconciled window) — 1.5 m at the baseline, 3.0 m at 300 ms. **Broadcast discovery leg also verified (this round)** on a real host NIC (/24): broadcast-only scanner found a `--network host` docker server via 255.255.255.255:7778; ENet join through the LAN IP with server-confirmed 20 Hz snapshot pacing (seq delta == client count over 10 s) — the final Phase 5 verification runnable without two physical phones (real-WiFi two-phone sign-off stays as the acceptance test). **Match lifecycle v1 also shipped (this round):** a finished match is reusable without a restart — a fresh join with no humans connected starts a new match in place (World.reset + reset_match + scene bot re-fill via match_reset; frozen tokens invalidated; joins with observers present rejected with the over code): sim suite 8/8 + live ENet (bot match over at t=55.9 -> joiner reset it in place, slot + 19.9 Hz seq-confirmed stream, discovery OVER->OPEN). This closes the last Phase 5 code/test items runnable on this machine. **Round 27 baseline (below) stands.** **Server ops v1 shipped (round 27):** the dedicated-server docker image (`server/Dockerfile`, repo-root build context; pinned Godot 4.7.2 release binary + game project + baked import cache) + the 2-core budget measurement (PERFORMANCE.md round-27 section): a full 3v3 bot match under `--cpuset-cpus="0,1"` runs at ~0.19 cores average (~10% of the 2-core budget), holds the 60 Hz fixed step at a world-time ratio of 1.0000, and streams snapshots at 19.9 Hz to a real ENet client through the published ports (199/200 in 10 s). Verified: discovery reply + ENet handshake + slot assign + snapshot pacing, both through the docker port mapping (host:17777/17778 -> container:7777/7778) and direct container-IP; `--network host` also works. The long container-debug detour ("published UDP port dead") turned out to be a port-mapping typo (host:17778 mapped to container:17778 while the server listens on 7778) — the userland proxy and bridge UDP were fine all along; the mapping gotcha is now documented in the Dockerfile + NETWORKING.md §9.4. Phase 5 exit is closer: remaining = real-WiFi two-phone broadcast leg, NAT traversal/relay, match lifecycle after `over` (new match), latency/loss 50/300 ms + 10% profiles. **Round 13 baseline (below) stands.** **Online play v1 shipped (round 13):** a separate headless lobby sidecar (UDP JSON-lines with seq/dedupe/retransmit — Godot 4.7 headless TCP creates no system socket, probed) + a 4-stage party-aware queue (strict → skill → region widen LATAM-first → botfill at 60 s) + hero-select ONLINE panel (region cycle, live ping, PLAY → queue → auto-join). Verified end-to-end on the emulators: app → lobby ping → PLAY → stage-1 assign in 0.0 s → in-match against the published 10.0.2.2:7777 server. Round-13 also fixed a client connect-state bug (stale-pong re-fire) and a 4.7 pre-add `global_position` error in the net client's view spawn. **Round 12 baseline (below) stands.** Two-device LAN verified: two Android emulators (different profiles/resolutions: 320x640-class + pixel_5 2340x1080) + a host-dedicated server — both devices SCAN-found the server, joined the same live match (team 0 / team 1), fought with a cross-device kill feed (a human headshot from device 2 rendered in device 1's feed), and both results overlays were correct per-side. That verification surfaced and fixed two real bugs: (1) the results overlay judged win/lose from a team-0 assumption (`main.gd`), so a net client on team 1 saw VICTORY when its team won — the client now computes lost/title from `my_team` and passes the score local-team-first (offline is always team 0 and keeps the fallback; offline re-verified on emulator: VICTORY 15-9); (2) a pending respawn lambda captured the CharacterEntity — when the yielding bot was freed mid-tick, 4.7 printed "Lambda capture ... freed, passed null" on the timer call (the round-11 one-off server error, finally reproduced); the respawn now captures the instance id (plain int) and looks the character up in `world.characters`, so a stale id is a no-op. **4.7 WeakRef note:** the class was reworked (property "RefCounted", `init_ref()`/get-ref with zero-arg constructors; `.ref`/`get_object()` no longer assign in GDScript) — another reason to avoid it for this pattern. **Headless battery green, 0 script errors** (11 suites incl. test_lobby); both APKs rebuilt with the round-13 code (baked renderers verified). Remaining for exit: broadcast leg on a real WiFi LAN (emulator NAT only reaches the unicast leg), internet NAT traversal/relay (published-addressing is v1), docker server ops + 2-core budget, more latency/loss profiles, party group-join handshake. (Round 12: 205/205 headless checks green across 10 suites; round 13 adds test_lobby, 12 checks.) **Two-device LAN verified:** two Android emulators (different profiles/resolutions: 320x640-class + pixel_5 2340x1080) + a host-dedicated server — both devices SCAN-found the server, joined the same live match (team 0 / team 1), fought with a cross-device kill feed (a human headshot from device 2 rendered in device 1's feed), and both results overlays were correct per-side. That verification surfaced and fixed two real bugs: (1) the results overlay judged win/lose from a team-0 assumption (`main.gd`), so a net client on team 1 saw VICTORY when its team won — the client now computes lost/title from `my_team` and passes the score local-team-first (offline is always team 0 and keeps the fallback; offline re-verified on emulator: VICTORY 15-9); (2) a pending respawn lambda captured the CharacterEntity — when the yielding bot was freed mid-tick, 4.7 printed "Lambda capture ... freed, passed null" on the timer call (the round-11 one-off server error, finally reproduced); the respawn now captures the instance id (plain int) and looks the character up in `world.characters`, so a stale id is a no-op. **4.7 WeakRef note:** the class was reworked (property "RefCounted", `init_ref()`/get-ref with zero-arg constructors; `.ref`/`get_object()` no longer assign in GDScript) — another reason to avoid it for this pattern. **205/205 headless checks green, 0 script errors, 0 lambda-capture warnings** (10 suites); both APKs rebuilt with the fixes (baked renderers verified). Remaining for exit: broadcast leg on a real WiFi LAN (emulator NAT only reaches the unicast leg), internet play + matchmaking prototype, docker server ops + 2-core budget, more latency/loss profiles.

## 6. Phase 6 — First Complete Match (FIRST MAJOR MILESTONE)

**Status: IN PROGRESS (round 38).** **All four modes + map picker + 2 original maps + results screen + progression v1 + next-match mode AND map voting (incl. per-party vote bundles) + cosmetics/mastery display + mobile controls customization shipped (D16-D24).** Match rules are data resources (`Mode` + TDM/Control/Capture/Escort + `ModeRegistry`) and MAPS are data too (`Map` + `content/maps/*.tres` + `MapRegistry`) — the server's step/over/score path is mode-generic (setup/step/check_over/score_of/on_kill) and `Arena.build(world, map)` builds geometry from the map data (empty map = legacy fallback). Modes: TDM, Control (central point, 15 s solo, contested freezes, held until re-captured, 2 to win, timeout tiebreaks), Capture/CTF (flags at the team bases, 1.5 m steal, drop-on-death, own-team re-secure, 2 to win, timeout -> captures -> enemy-flag holder -> draw, + objective pressure: a stale free flag > 8 s pulls the designated teammate off their fight), Escort (lane payload, head-count push/brake, delivery = attacker win, timeout = defender win). Maps: Crossdocks (44 m) + The Foundry (52 m, center core ring) — both live-verified on ENet. Player picks the match: the hero-select MODE/MAP row writes MatchConfig; M_SLOT carries mode_code + map_code (client mirror arena); the lobby reg/assign broadcast the mode. RESULTS (D19): server-side per-character stats (kills/deaths/damage, sacred authority) + M_STATS wire (before match_over) + results screen with mode/map label, K/D/DMG table, MVP and an XP line; PROGRESSION v1 is local and cosmetic (ProgressionConfig content data + PlayerProfile user:// JSON: level curve, win/kill/MVP XP, per-hero record) with a hero-select badge — no P2W. Bots: the CAPTURE intent dispatches per mode. VOTING (D20/D21/D23): lobby protocol v1.5 (vote/voteresult/setmode + mapvote/mapvoteresult/setmap with optional party-bundle fields), last-write-wins WEIGHTED tallies in TWO INDEPENDENT domains (mode, map) - a party speaks through its leader (the leader's vote carries party_size as weight, clamped 1..6; members are acked with party_vote and add none), decision = strict weighted majority AND >= 2 voting entities (a lone party cannot self-decide, equal parties tie and hold), solo (no party_id) votes keep weight 1 so all-solo lobbies behave exactly like v1.4, directory mode/map flip, the server applies the voted mode (pure-data swap) and map (arena rebuild + spawn-point change) at the next in-place match reset, results-screen vote UI (mode + map rows with live tally lines). COSMETICS (D22): per-hero MASTERY as a pure function of the stat seed (data weights in ProgressionConfig, shared level curve; the server never reads it) + cosmetic VARIANT palettes (content/cosmetics/hero_variants.tres: 5 original colors per hero, mastery unlock schedule [0,2,4,6,8], palette[0] pinned to the hero's default color); selection persists in the profile, hero-select shows the mastery line + tappable variant dots + the card preview in the chosen color, and the player character (offline + net) wears the selected variant; locked picks clamp to the highest unlocked — strictly cosmetic, no P2W. Sim suites: control 15, capture 17, escort 11, map 12, results 18, vote 13, mapvote 12, cosmetics 21, partyvote 20, controls 25 — full 24-suite battery GREEN (426 checks, quiet run). CONTROLS (D24): the touch HUD layout is data x settings — ControlLayout baseline (content/settings, the Phase 1 layout as viewport fractions) x user ControlSettings (aim sensitivity, button scale/opacity, joystick scale, fire-side mirror; clamped, persisted in the profile, old saves = stock), resolved by the pure ControlSettings.effective() the TouchControls builds from (desktop mouse shares the aim sensitivity); hero-select CONTROLS panel (steppers + toggle + reset). Strictly ergonomic, no P2W. Remaining in Phase 6: real-device perf sign-off (Foundry = the 52 m stress map), public demo build + issue tracker. (The party group-join handshake + party UI are v2 follow-ups of D23 - parties are a party_id convention for now, matching the lobby's unauthenticated trust model.)

**Deliver (directive §6, verbatim):**
- [x] 3v3 competitive + casual (TDM + Control + Capture + Escort available) - D16-D17, mode picker + voting
- [x] 2 original maps (compact, lanes, cover, verticality, flanks, objectives, spawns) + map voting in lobby - D18/D21
- [x] 6 heroes, fully unlockable (no power gating — directive §7) - roster verified headless (test_roster)
- [x] Bots (4 difficulties) usable offline and as queue fill - D3/D4; emu-verified as queue fill
- [x] Mobile controls (customizable HUD: position/size/sensitivity/opacity/aim settings) - D24
- [x] 1 complete game mode end-to-end (match flow: lobby → countdown → play → results → rewards → requeue) — the others follow from the mode framework
- [x] Online/LAN multiplayer (from Phase 5) - D12-D15: discovery, lobby, relay
- [x] Match length 3–8 minutes (timer + kill targets tuned) - content/balance owns duration/targets
- [x] Results screen (stats, MVP, rewards) + basic progression (account level, hero mastery, cosmetics slots) - D19/D22
- [ ] Real-device perf pass: low-end 30 / mid 60 on all four maps (PERFORMANCE.md sign-off)
- [x] Public demo build (APK + PC) + open issue tracker live - round 39: skyvanguard/hero-arena, release demo-0.1.0, docs/BUILD.md

**Exit criteria = directive §45 "definition of success," verbatim:** a user installs the APK, launches, chooses a hero, enters 3v3 (humans or bots), moves, shoots, uses abilities, coordinates, understands the objective, dies, respawns, wins or loses, in ~3–8 minutes, smoothly on real Android.

## 7. Phase 7 — Systems

- [x] **Perks** (in-match level 2/3, 2 choices each; data-driven; ult modifiers as extension) - D25: PerkSystem + PerkPool content, server-authoritative, bots + wire (v1.6), 50-check battery (38 sim + 12 UI); ult charge modifier included
- [x] **Role/sub-role passives** tuned for the full roster (registry proven in Phase 3) - round 41: all six passives behavior-verified in the real pipeline (test_passives, 25 checks: physics speed, damage mitigation, cooldown timing, aura gates, tick-heal vs control world, streak decay, passive x perk x ult multiplicative stacking, identity uniqueness)
- [x] Additional modes + map pool growth (2 more maps) - round 42: The Sawmill (48 m flanking corridors + central ridge) and Saltline (56 m open + elevated corners) as pure Map data; test_map now validates the whole pool (17 checks: integrity, bounds, mode-follow, 6-bot fight on the new map)
- [x] Progression v2 (hero mastery, achievements, seasonal cosmetics — cosmetic only) - round 43: D26 - 10 data-driven achievements (conditions over server-stat-derived profile counters, one-shot, cosmetic rewards incl. early variant unlocks) + seasonal cosmetic packs (SeasonData/SeasonBank, Season 1 ships, current-season is content); mastery (D22) regression-tested; test_progression_v2 (23 checks); results overlay shows new achievements + season label
- [ ] Monetization-ready shop architecture (optional; cosmetics-only rule enforced by content schema)
- [x] Map voting polish (anti-repetition rotation) - round 44: D27 - the lobby rejects re-voting the entry's current (just-played / about-to-play) map while alternatives remain (`MapRegistry.rotation_pool`); `mapvoteresult` carries a `repeat` marker and the results UI grays the last map's button; test_mapvote 17 checks incl. rotation (the older map becomes votable again) + party-leader repeat gate; test_partyvote map scenarios updated to the rule
- [x] Matchmaking v2 (win-probability model, grouping-advantage handling, region prioritization for LATAM) - round 45: D28 - data-driven MMConfig (content/matchmaking.tres); the SKILL stage now gates on a projected even-split team delta (fairness band 250, neutral bot rating, proportional skill-ledger decay on departures); parties projected as blocks (grouping advantage); REGION stage prefers fair matches inside the LATAM-priority widen order; T_ASSIGN carries the win_prob estimate (shown in the queue UI); v1 skill-0 clients queue exactly as before; test_matchmaking_v2 (14 checks)
- [ ] AI coaching (optional post-match tips from authoritative stats)
- [x] Events framework (participation/win/objective rewards; no hero-forcing) - round 46: D29 - data-driven EventData/EventBank (content/events/events.tres): goal kinds MATCHES (participation) / WINS (victory) / MVP (objective performance) + optional mode filters + one-shot completion + cosmetic-only variant rewards (3 shipped events: Forge Week, Capture Push, Objective Sprint); no-hero-forcing rule (eligibility is roster-wide); apply_match gains an optional mode_id (old call sites unchanged); results overlay shows EVENTS COMPLETE; test_events (15 checks)

**Exit criteria:** perks create *choices* not just numbers (playtest metric: ≥2 different picks across 10 players on same hero); coaching tips are actionable; event runs headless in CI.

## 8. Phase 8 — Community

- [ ] Dedicated server release (docker, one command: docker run …/hero-shooter-server)
- [ ] Mod support v1 (heroes/maps/weapons/abilities/modes/cosmetics/balance drop-in; validation tool; versioned mod API)
- [ ] Custom match hosting (private servers, lobby API, room codes)
- [ ] Contribution workflow (branch model, PR templates, CODE_OF_CONDUCT, asset-license checklist)
- [ ] Docs complete (CONTRIBUTING, NETWORKING, MOBILE, PERFORMANCE all current; architecture walkthrough video or GIFs)
- [ ] First community mod published to the mod index

**Exit criteria:** a stranger can clone the repo, build, run a server, and load a community hero mod in <1 hour (measured with a volunteer, documented).

---

## Cross-Phase Guardrails

1. **Android test at every phase exit** (directive §25): no phase closes without a real-device check.
2. **Open source at every commit** (directive §32): code Apache-2.0; assets individually licensed; no proprietary dependencies without a review note.
3. **Fun is the metric** (directive §46.1): every phase ends with a playtest note (who played, what felt good, what felt bad) in the milestone doc.
4. **No scope creep without a decision log entry** (ARCHITECTURE.md §4 format).
5. **Bots are tested like players** (directive §41): CI bot eval from Phase 4 onward.
6. **3v3 stays tuned** (directive §3): any team-size change goes through balance blocks, never hardcoded.

## Milestone Timeline (indicative, not a promise)

| Milestone | Phase | Gate |
|---|---|---|
| Vertical slice on Android | 1 | go/no-go engine check |
| First hero | 2 | kit legibility |
| 6 heroes + practice range | 3 | identity test |
| Smart bots, offline 3v3 | 4 | "fun alone" guarantee |
| LAN + internet multiplayer | 5 | authority tests green |
| **First complete match (major)** | 6 | directive §45 verbatim |
| Perks/progression/coaching | 7 | choice + fairness checks |
| Community servers + mods | 8 | stranger-onboarding test |
