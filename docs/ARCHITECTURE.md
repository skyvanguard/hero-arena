# Architecture

**Status:** Phase 0 design (compiled 2026-09-02)
**Engine:** Godot 4.7.x (see ENGINE_DECISION.md) · **Primary language:** GDScript (C# for measured hot paths) · **Model:** authoritative server, same codebase headless
**Scope:** this doc defines the system architecture, module boundaries, data model, and key decisions. Detailed networking is in NETWORKING.md (to be expanded in Phase 5), performance in PERFORMANCE.md, gameplay numbers in GAME_DESIGN.md (Phases 1–2).

---

## 1. System Overview

text:
  Player (Android/PC)
    │  input (touch/KB)            ▲  rendering + feedback
    ▼                              │
  Client (Godot, renderer ON)
    │  actions (predicted)         │  snapshots (interpolated)
    │  state queries               │  events (damage, kill, VFX cues)
    ▼                              ▼
  Network (ENet reliable/unreliable, WebRTC for future)
    │
    ▼
  Match Server (Godot, headless, 1 process per match)
    │  authoritative simulation @ 60 Hz (fixed timestep)
    ▼
  Match State: players, bots, heroes, weapons, abilities, objectives, mode logic

  Services (outside match process, Phase 5+):
    - Lobby/Matchmaking service (queue strategy, bot fill, regions)
    - Account service (local-first; optional cloud sync)
    - Content registry (heroes/maps/modes/perks — data-driven, moddable)

Invariants (directive §21 — "never trust clients"):
- Damage, kills, ammo, cooldowns, movement validity, score, objective state: **server-authoritative**.
- Clients send **inputs/actions** (move vector, aim, fire events, ability casts, reload). They may **predict** locally for feel; the server reconciles (see NETWORKING.md).
- Bots run **server-side** in the same simulation (a bot is just a controller feeding the same interfaces a human client uses — this guarantees human/bot parity, critical for the bot-as-feature goal).

## 2. Repository Layout (Godot-adapted)

text:
  /
  ├── AGENTS.md                 # AI development rules (from directive §40)
  ├── README.md                 # project overview + quickstart
  ├── LICENSE                   # code license (Apache-2.0, decided in Phase 1 commit)
  ├── CONTRIBUTING.md
  ├── docs/
  │   ├── T3_RESEARCH.md  OVERWATCH_RESEARCH.md  OPEN_SOURCE_RESEARCH.md
  │   ├── ENGINE_DECISION.md  ARCHITECTURE.md  ROADMAP.md
  │   ├── NETWORKING.md  MOBILE.md  PERFORMANCE.md  GAME_DESIGN.md
  ├── game/                     # THE Godot project (client + server share this tree)
  │   ├── project.godot
  │   ├── core/                 # engine-agnostic-ish foundations
  │   │   ├── net/              # transport, snapshot codec, prediction, reconciliation
  │   │   ├── state/            # entity state, fixed-timestep world, events bus
  │   │   └── util/             # pools, math, config loading, save/load
  │   ├── gameplay/
  │   │   ├── character/        # base ThirdPersonCharacter, animation, movement
  │   │   ├── combat/           # weapons, damage pipeline, status effects, hit registration
  │   │   ├── abilities/        # ability framework (casts, projectiles, zones, ult)
  │   │   ├── heroes/           # hero definitions + per-hero scenes (data-driven)
  │   │   ├── bots/             # controller stack: perception→decision→execution, difficulty
  │   │   ├── modes/            # mode rules (TDM, Control, Capture, Escort, Clash-like)
  │   │   └── objectives/       # objective entities (points, crystals, payload)
  │   ├── content/              # DATA-DRIVEN content (Resources/.tres + JSON exports)
  │   │   ├── heroes/  weapons/  abilities/  perks/  roles/
  │   │   ├── maps/   modes/    balance/
  │   ├── ui/
  │   │   ├── hud/              # in-match HUD (health, ammo, abilities, objective)
  │   │   ├── touch/            # virtual joystick, buttons, customizable layout
  │   │   └── menus/            # main, hero select, settings, results
  │   ├── audio/  vfx/  characters/  maps/  (asset folders, separately licensed)
  ├── server/                   # dedicated-server build config, docker, ops tooling
  │   ├── Dockerfile  compose.yaml  regions.yaml
  ├── mods/                     # community content drop-in (Phase 8)
  │   ├── heroes/  maps/  weapons/  abilities/  modes/  cosmetics/  balance/
  └── tests/                    # headless Godot tests (GUT or built-in), net sim, bot eval

Rules:
- gameplay/ must run identically headless (no rendering-only code outside ui/vfx).
- All balance numbers live in content/balance/ (Resources) — no magic numbers in logic.
- mods/ mirrors content/: a mod is a folder that overrides/adds data (+ optional scripts with a versioned API).

## 3. Core Modules

### 3.1 Fixed-timestep World (core/state)

- Authoritative simulation ticks at **60 Hz** (server), deterministic-adjacent: same inputs → same state (we do not require full determinism, but we require *idempotent* actions with sequence numbers for reconciliation).
- Entities: CharacterEntity (player or bot), ProjectileEntity, ZoneEntity, ObjectiveEntity, PickupEntity, VFX cue. Each has:
  - "state" (serializable snapshot fields),
  - "inputs" (what a controller may send),
  - "events" (emitted for clients: hit, kill, ability_cast, objective_change, respawn).
- Event bus: server publishes typed events; net layer forwards them to clients (reliable channel for game-meaningful events, unreliable for cosmetic).

### 3.2 Character & Movement (gameplay/character)

- Base ThirdPersonCharacter (pattern from gdquest-demos/tps-demo, rebuilt original): capsule body, grounded/airborne states, coyote time, jump buffering, slope handling, air control.
- **Role passives and sub-role passives attach as components** (data-driven, §3.5) — core movement code never special-cases a hero.
- Per-hero movement identity (dash, double jump, flight, slide) = movement components configured by the hero definition.
- Mobile input contract: left stick (move), right stick (aim/camera), discrete buttons (jump, reload, ability1/2, ult, interact). Input abstraction makes desktop (KB/mouse) a second binding of the same contract.

### 3.3 Combat (gameplay/combat)

- Weapon model (per hero, from T3 research: every hero has a unique primary):
  - Hitscan (instant raycast, server validates against interpolated positions) or Projectile (spawn ProjectileEntity; client-side lead assist optional).
  - Ammo/reserve, reload timing, weapon heat/charge where applicable (Sharpshooter-style data tables — OW research §2.2), fire modes, headshot multipliers, critical rules.
- Damage pipeline (server): source → target → armor/shield split → modifiers (role passives, perk multipliers, status effects, zone effects) → final damage → HP → death check → event.
- HP/armor: **regen-to-cap after N seconds without damage + map health pickups** (T3 pattern, §8.12); overhealth (armor) for tanks.
- Status effects: data-driven (slow, burn, shield, stun, knockback with impulse, invisibility) with durations/stacking rules in content/balance.
- Crowd control budget: CC durations are global config (prevent CC-lock; documented in GAME_DESIGN.md when numbers are set).
- Respawn: configurable timer (target 5–10 s), spawn protection window, objective-aware spawn logic (never spawn on top of the enemy team).
- Kill feed, damage arrows, hitmarkers, elimination feedback — client-side feedback system listening to server events (§3.9 UI).

### 3.4 Ability Framework (gameplay/abilities)

A unified, data-driven framework; heroes are *compositions*, not code:

- Ability definition (.tres Resource):
  - id, hero(s), type (active/ult/passive/projectile/zone/movement/buff),
  - cost (cooldown / ammo / charge / resource),
  - parameters (radius, duration, damage, impulse, projectile speed…),
  - effects list (damage, heal, cc, buff, spawn_projectile, spawn_zone, teleport/dash, shield),
  - tags (for perk targeting, bot decision, cooldown-grouping),
  - i18n strings + VFX/SFX refs.
- Ultimate: **combat-driven charge** (damage dealt + healing, T3/OW pattern), global max, charge modifiers (e.g., support role passive from assists — data-driven, §3.5).
- Passive: always-on components (movement modifiers, damage modifiers, trigger-based: on_kill, on_damage_taken, on_level_up).
- **Perks (directive §8, OW model):** in-match level 1→2 (minor) →3 (major); each level offers **2 choice cards**; choice modifies ability parameters (e.g., projectile speed, reload speed, duration, +effect). Perks are **data**: a perk = a patch applied to an ability/weapon parameter table. Optional modifiers (ult modifiers) extend the same mechanism. Data-driven ⇒ trivially extensible and balance-testable headless.
- **Role / sub-role passives (directive §9):** a registry maps (role, sub_role) → passive component definitions. New role passives = new data, zero core-combat changes (OW sub-role proof: §2.2 of OVERWATCH_RESEARCH).

### 3.5 Hero Definition

- Hero = Resource referencing: role + sub_role, weapon id, ability ids (active1, active2, ult, ult2?, passive), movement components, kit balance block, cosmetic slots (original art only).
- Roster launch: **6 heroes** (2 Assault, 1 Tank, 2 Support/Utility, 1 Controller — per directive §6), each with a distinct movement/combat identity. Identities designed in GAME_DESIGN.md (Phase 2); the *shape* of the kit (weapon + passive + actives + ult + movement identity) is fixed by this architecture.
- No per-hero power levels: unlock = full kit (directive §7). Cosmetics attach at render level only.

### 3.6 Bots (gameplay/bots) — core feature, not placeholder

Server-side controller stack (identical interface to a human client):

text:
  Perception → Decision → Execution → Memory
  (vision/hearing/ (utility scoring +  (movement, aim,  (last seen,
   objective state,  behavior trees    ability timing,   teammate/enemy
   ally state,      per difficulty,   cover seeking,    intentions, own
   own state)       team roles)       objective pursuit)  cooldowns)

- **Objective-aware**: bots score behaviors against the *current mode's* goal (hold point / push payload / kill count), not just "seek enemies" — directive §13.
- **Team behavior**: role-based responsibilities (tank holds space, support stays with group + heals, controller denies, assault trades), regroup/flank logic, revive/protect rules, callouts via the same event bus players hear.
- **Difficulty tiers** (Beginner → Normal → Advanced → Expert) = parameter packs: reaction time, aim error, decision depth, objective weight, info (beginner bots see less — fair by design). Tiers are *data*.
- **Bot difficulty also scales with population**: offline mode = full bot match; online queue fill = mixed, always labeled to the player (transparency, §17).
- Evaluation harness: headless bot-vs-bot and bot-vs-player match runner producing stats (objective time, K/D, ability usage) for CI regression (directive §41).

**As built (Phase 4, round 7):** `BotController` (gameplay/bots/) composes
`BotPerception` + `BotDecision` as children and is added to the hero exactly
like a human input source — `setup(hero, target, world, difficulty)` +
`step(world, dt)`, driven inside `CharacterEntity.step` so bots need no
external pulse. Perception: per-tick vision cone (FOV + range + LOS raycast
against LAYER_WORLD|BODY|HEAD) plus hearing from the world `shot` event,
keeping a `known` table (last position + time) pruned after ~8× the
difficulty's `lost_sight_timeout`. Decision v1 is an intent ladder
(RETREAT → ATTACK → INVESTIGATE → REGROUP → HOLD) with role multipliers
(tank: closer + holds the line; support: regroups harder, fights at range)
and a **retreat-confirmation window** (`retreat_confirm` seconds below
`retreat_hp` before committing, re-engage at `retreat_hp + 0.15`) — without
it, per-tick retreat at the threshold made bots mathematically unkillable by
focused fire. Execution: strafe cadence + approach/hold at a role-biased
fight range; aim error is an **angular** cone (direction rotated by a random
deviation ≤ `aim_error_deg`, resampled at 5 Hz) aimed from head height
(`CharacterEntity.AIM_HEIGHT`) — a fixed meter offset made accuracy
independent of range; auto-reload (`start_reload()` when the clip is dry in
combat, pre-load out of combat). Difficulty packs are `BotDifficulty`
Resources in `content/balance/bots/` (reaction, aim_error_deg, ranges,
strafe, vision/hearing, lost-sight timeout, scan speed, ability_quality,
grouping threshold, retreat parameters) selected via `BotDifficulties.by_id`
and exposed in the UI through the `MatchConfig` autoload (`difficulty`,
`team_size`). Eval harness: `tests/test_bots.tscn` (13 checks incl. live 3v3
"kills happen" and expert-out-K/Ds-beginner). Two core-system fixes came out
of the bot eval: (1) the head hitbox is a **StaticBody3D** (`HeadSensor`)
— Godot 4.7 `intersect_ray` does not report `Area3D`, so an Area head made
heads unhittable and headshots silent; headshot detection walks the collider
chain for `HeadSensor` (`CharacterEntity.hit_is_head`); (2) ray queries must
exclude the *whole character* (`CharacterEntity.own_rids()` = body + head
sensor) — a query started inside the shooter's own head sensor blocked all
LOS. See PERFORMANCE.md for the HUD 2D-canvas memory rule (write-on-change +
pooled kill feed) that 3v3 runs required.

Team behaviors v1 (round 8): **flank spread** (bots sharing a target offset
their ATTACK goal laterally by `flank_spacing` × rank, rank = distance to
target, so a squad doesn't stack on one line of fire) and **stick/protect**
(an idle bot more than `stick_range` from an in-combat ally — attacking, or
took damage < 2 s — REGROUPs toward it). Both are data-driven (difficulty
pack) and headless. TDM **match end** lives in the authoritative `World`
(first of: a team reaching `target_score`, or the `match_duration` clock;
`match_over` emitted once, sim freezes after) with pacing values in
`MatchConfig` (directive §6: 3–8 min matches). The client renders a results
overlay (VICTORY/DEFEAT/DRAW + score + duration) whose own background takes
the dismiss tap — the in-match touch layer's STOP full-rect zones would
otherwise swallow it.

### 3.7 Modes (gameplay/modes) — pluggable rules

- IMode interface: on_match_start / on_tick / on_event / win_condition / scoring / respawn_policy / spawn_logic.
- Launch modes: **Team Deathmatch** (kill target), **Control** (hold to %), **Capture Point** (multi-point sequence), **Escort/Payload**. Architecture: a mode is data + a rules script; new modes = new folder in content/modes (+ optional script).
- Team size is a **configuration** (1v1…6v6), not code: spawn counts, objective sizing, and balance blocks are parameterized by team size (directive §3). 3v3 is the tuned default; other sizes ship when balance blocks exist.
- **Map voting** (directive §24): lobby-level feature (weighted vote + rotation to avoid repetition), mode-agnostic.

### 3.8 Maps

- Launch: **2 maps**, compact (T3-scale), multi-lane, cover, verticality, flank routes, readable sightlines, objective space, distinct spawn points (directive §23).
- Map = scene + config: spawn points per role, objective anchors, pickup placement (health potions — T3 pattern), navmesh (bots + AI pathing), cover points (bot decision data), readout for mobile (visual density limits).
- Nav data is precomputed and versioned with the map.

### 3.9 UI / Touch (ui/)

- **Mobile-first HUD** (directive §11, §27): left virtual joystick, right aim stick, fire/ability1/ability2/ult/reload/jump/interact buttons — all **repositionable/resizable** (persisted layout), sensitivity/opacity/aim settings, per-device aspect handling (notch, safe areas, landscape-first).
- HUD shows: HP/armor, ammo, ability cooldowns + ult charge, objective state + direction, teammate pips/health, enemy indicators, kill feed, damage arrows, hitmarkers — clean and glanceable (readability priority).
- Menus: main (Play/Practice/Heroes/Settings/Community), hero select (role filter, hero intro), results (stats + rewards + coaching tips), settings (controls, audio, graphics presets, aim, region/ping display).
- **AI coaching (directive §14, optional, Phase 7+):** post-match analysis from server-side stats (objective participation %, deaths vs team, ability uptime) → short text tips. Computed server-side (stats are authoritative), rendered client-side.

### 3.10 Networking (summary — detail in NETWORKING.md)

- Transport: Godot high-level multiplayer (ENet) to a dedicated headless server; WebRTC path reserved.
- **Snapshots** @ 20–30 Hz (unreliable-ordered): entity positions, rotations, HP, ammo, cooldown states, objective states.
- **Events** @ reliable: casts, hits, kills, objective changes, respawns, perk unlocks.
- **Client prediction** for local movement + fire rate; **reconciliation** on snapshot arrival; **interpolation** (~100 ms buffer) for remote entities; **lag compensation** (server rewinds 100–200 ms for hitscan validation); **hit validation** with per-player tolerance (aim-assist-eligible windows).
- **Reconnect**: session token + state resync (full snapshot + recent events).
- **Matchmaking** (lobby service): party-aware queue (directive §15/§16) — strict skill+latency → widen skill → widen region → **bot fill at 60 s+** (configurable thresholds); displays Region/Ping/Server/skill band/party composition on match found (§17).
- **As-built v1 (round 9, detail in NETWORKING.md):** transport = ENetMultiplayerPeer driven through Godot 4.7 `SceneMultiplayer` (a RefCounted MultiplayerAPI — not a Node; requires `set_root_path` + manual `poll()`); own wire codec (magic-byte dispatch, LE, f32 bit codec); humans are server-side `NetPlayerController` instances (controller parity with bots, D3); client views are physics-invisible (shared physics space in loopback).
- **As-built v1.1 (round 10, detail in NETWORKING.md):** protocol v2 (hello session u32, slot token u32 + match-over result, input time_est f32); **server input sanitization + u16 wrap-aware seq gate** (move/aim clamped, stale frames and stale edges dropped — property-tested); **lag-comp hitscan** = single `world.hitscan()` truth (current physics ray + analytic 3-sphere rewind over a 60 Hz pose history, window 200 ms, delay from client time_est); **client prediction** = private World + twin character driven by the real NetPlayerController from the same 20 Hz samples (D3 parity extended client-side) with hard-snap reconciliation above 0.35 m; **reconnect** = slot freeze + token reattach (same CharacterEntity) with bounded client retry; **SimLink** = swappable transport (per-direction latency/loss) for the net-sim suite (150 ms RTT + 2% loss green). LAN discovery + internet: later Phase 5 rounds.
- **As-built v1.5 NAT traversal: relay (round 29, detail in NETWORKING.md §9.5, D15):** a server behind a NAT registers in a standalone relay (core/net/relay.gd, res://net/relay.tscn, control 7800) over an OUTBOUND UDP connection — `R_REG` carries the game port, the relay grants a **virtual port** 7901..8156 (`R_OK`), and the client's normal ENet connection to `<relay_ip>:<vport>` is pumped headerless: one ephemeral socket per (match, client) so the server sees distinct client source ports and the client sees the server at the dialed vport (ENet drops replies from any other source port). ENet reliability/flow control run on the forwarded stream; R_PING (10 s) holds the NAT mapping, eviction at 60 s (match) / 120 s (link). The **client protocol is unchanged** — the lobby simply advertises the vport (server `--relay=ip:port` defers lobby registration until the vport is granted; LAN discovery is unaffected). Verified: 7-check suite (two relayed matches in parallel, no cross-talk, reconnect through the relay) + live bridge-container NAT test + full lobby→assign→join flow. The docker image moved to ENTRYPOINT+CMD so run args select the scene (relay runs from the same image).
- **As-built v1.15 in-match perks (round 40, D25):** in-match levels are server-authoritative and data-driven. `PerkSystem` (Node attached to the World, `world.perk_system`; `gameplay/perks/`) consumes the world's own events (hit / kill / heal) and grants XP per the `PerkPool` content resource (`content/perks/perks.tres`: rates, level-2/level-3 thresholds, 12 PerkData perks in two tiers with role filters). On level-up it rolls TWO distinct role-filtered offers seeded per (match seed, character index, tier) and holds them pending; `pick(ch, idx)` validates and applies the perk's multiplier table onto `CharacterEntity.perk_mults`, which the existing modifier pipeline consumes (`AbilityComponent._pm()`: damage / fire_rate / cooldown / speed / spread / charge; regen and max_hp on the entity). No new action vocabulary on the wire beyond the input edge bits 32/64 (perk pick 0/1, sanitized like every other edge) plus one E_PERK game event (level + choice pool indices + picked) and a TRAILING 5-byte-per-char perk block in the snapshot (level + picked/pending pool indices; absent on older servers -> clients read `perk_extra = []`): protocol v1.6, detail in NETWORKING.md. Bots pick through the SAME `pick()` entry point (BotController, after a 0.75 s visible delay; controller-less characters auto-pick in `grant()`), per the controller-interface rule. The client's mirror world is render-only for perks (UI state arrives via E_PERK + snapshot; the client loads the same pool resource purely to resolve names). Practice range deliberately has no PerkSystem (it trains the kit, not the match). 38-check suite `tests/test_perks` (XP curve, role filter, determinism, every effect key, bot picks, reset, wire round-trips incl. backward-compat).
- **As-built v1.14 mobile controls customization (round 38, D24):** the touch HUD layout is now DATA x SETTINGS, not code. The baseline layout (button positions as viewport fractions, radii, colors, joystick radius, touch aim sensitivity, aim-zone split) lives in a ControlLayout Resource (content/settings/control_layout.tres - the old Phase 1 hard-coded layout, lossless migration); the user's customization (aim sensitivity 0.25-3.0, button scale 0.75-1.5, button opacity 0.3-1.0, joystick scale, fire-side mirror) is a ControlSettings object persisted in the player profile JSON (old saves = stock). ControlSettings.effective(layout, settings, vp) is a PURE resolver: the in-match TouchControls builds from it (position/size/opacity/sensitivity all customizable), the desktop mouse path multiplies LOOK_SPEED by the same aim sensitivity (static data, gameplay/ stays UI-free), and the hero-select CONTROLS panel (steppers + fire-side toggle + reset, bottom-right button) edits it with immediate persistence (same pattern as D22 variant taps). No wire changes - the aim delta was already client-scaled, and settings are cosmetic/ergonomic, no P2W (detail in NETWORKING.md §3.2).
- **As-built v1.13 per-party vote bundles (round 37, D23):** lobby protocol v1.5 - the vote tally is now WEIGHTED. A party speaks through its leader: the leader's vote (vote/mapvote with party_id + party_size + leader=true) carries the declared party size as weight (clamped 1..6); non-leader members send the same party_id with leader=false and are acknowledged with party_vote=true without adding weight. Decision rule: a STRICT WEIGHTED MAJORITY (winner*2 > total) with at least TWO voting entities (peers) - a single party, however large, cannot unilaterally decide its own domain, and equal parties (3v3, 2v2) tie and hold. Solo voters (no party_id) keep weight 1, so all-solo lobbies behave exactly like v1.4 and v1.4 clients are solo voters on a v1.5 lobby. Party context travels with the vote (assigned peers leave the waiter queue, so a per-waiter identity would be lost). Directory/assign tallies are the weighted counts; setmode/setmap forwarding and sticky per-domain decisions are unchanged (detail in NETWORKING.md §9.2).
- **As-built v1.12 cosmetics + mastery display (round 36, D22):** per-hero MASTERY is a pure function of the per_hero stat seed (plays/wins/kills/mvp) weighted by ProgressionConfig data (mastery_match/win/kill/mvp) and leveled on the SAME curve as the account level - no separate XP store, so old saves and re-rolls can never drift (pre-D22 saves load with mvp=0). Cosmetic VARIANTS are data (content/cosmetics/hero_variants.tres: HeroVariantBank of per-hero HeroVariantSet resources; 5 original palette colors, unlock schedule [0,2,4,6,8] mastery levels, palette[0] pinned to HeroData.color and verified by test). Selection lives in the profile (variants: hero_id -> index) and resolves through HeroVariantBank.color_for, which clamps a locked pick to the highest unlocked index. Display: hero-select mastery line per card + tappable variant dots (locked = dimmed/inert) + the card preview in the selected color; the player character wears the variant in offline, practice and net play (MatchClient.setup takes the player color; bots/others keep the identity color - opponents' variants are not in the wire in v1, which is why the net client tints only its OWN predicted character). Server authority untouched: the profile and bank are client-side, nothing here gates gameplay (no P2W).
- **As-built v1.11 next-match map voting (round 35, D21):** the lobby vote machinery (D20) is now domain-generic: mode and map are two INDEPENDENT tallies per match (a peer can hold one mode vote and one map vote at once; each decides on its own strict majority, each sticky until re-registration). Wire v1.4: mapvote/mapvoteresult/setmap, reg and state carry the map, assign carries map + map vote state. The map domain's application point differs from mode's: the arena is NODES, so the scene (server_main) applies the voted map in its reset handler - free the old arena, Arena.build(world, MapRegistry.get_map(id)), update the server's map_id, then the usual bot re-fill reads the new spawn points. No client protocol change: M_SLOT map_code already tells a reconnecting client which map to mirror. Results-screen UI: a second "NEXT MATCH - VOTE THE MAP" row (display-name buttons + live tally line), same fire-and-forget lobby connection as the mode row. Sim-verified: test_mapvote 12/12 over real UDP loopback (host map in directory, bad-map rejection, idempotent re-votes, mode/map independence, split holds, majority decision + setmap, arena rebuild with spawn-point change at reset, assign vote state, heartbeat freshness).
- **As-built v1.10 next-match mode voting (round 34, D20):** the lobby (protocol v1.3) is now the voting venue for a match's NEXT mode: clients send `vote {match_id, mode}` (fire-and-forget); the lobby keeps one vote per peer per match (last write wins - a new vote retracts the previous, so re-taps and retransmits never inflate the tally) and acks with `voteresult {tally, leading, decided, mode}`; a decision needs >= 2 votes and a STRICT majority, is sticky, and resets on re-registration. On decision the lobby updates the directory entry's mode, adds tally/leading/decided to every `assign`, and forwards `setmode {match_id, mode}` to the registered match server. The server stays the rulekeeper: `MatchServer.set_mode_from_lobby()` validates the id against ModeRegistry.ids() and stores it as _pending_mode; `reset_match()` (the in-place new-match path) swaps `world.mode` to the voted Mode resource and runs its pure-data setup() before the scene re-fills the bots - a live match never changes rules mid-fight (v1 application point; in-match pre-start application and MAP voting are v2). UI: the results screen (net + lobby matches only) shows a "NEXT MATCH - VOTE THE MODE" row - four mode buttons plus a live tally/DECIDED line driven by the voteresult acks; the vote lobby connection is created on match end and freed on continue. Over matches are votable (the vote targets the next in-place match; the directory entry and its vote cycle persist across resets), and the server's 2 s state heartbeat (which also carries the running mode) keeps the entry alive past the 5 s lobby reap during long silent bot-only stretches. Verified: test_vote 13/13 over real UDP loopback (registration, vote validation, idempotent re-votes, split holds, strict-majority decision + setmode forwarding, server pending mode, reset application, assign vote state, heartbeat mode, over-match votes) + on-device (emulator): results-screen vote row renders, CAP tap, lobby ack rendered live as "CAPTURE 1 · leading CAPTURE".
- **As-built v1.9 results screen + progression v1 (round 33, D19):** the match end is now a real results screen, not a score toast. SERVER-side stats (sacred authority): `CharacterEntity.kills/deaths/damage_dealt` accumulate in `World.kill` and `CharacterEntity.apply_damage` (damage tracks the final applied amount, post spawn-protection and passives); `World.stats_rows()` emits `[team, kills, deaths, damage]` in `world.characters` order — the SAME index convention the snapshot char list uses, so the wire needs no slot mapping; `World.mvp_index(rows)` is a pure function both sides can run (most kills, damage tie-break, lowest index, -1 when no kills). Wire: `M_STATS` (0x54) — n + 5 bytes/char, sent by MatchServer on match_over BEFORE the over event (separate reliable datagram). The client stores the rows and the `ended` signal now carries them; the offline path builds the same rows straight from the world. UI: `_show_results` gained the mode/map label, a per-player table (hero names, K/D/DMG, MVP starred, team-colored) and an XP line; the hero-select screen shows a local progression badge (LV · XP · matches · wins). PROGRESSION v1 is cosmetic and client-only (the server never reads it): `ProgressionConfig` (content/progression.tres — xp_win/xp_lose/xp_kill/xp_mvp + level curve base×growth^n, data per the no-magic-numbers rule) + `PlayerProfile` (user://profile.save JSON: level, xp, totals, per-hero plays/wins/kills = the mastery seed); `apply_match()` returns the gain + level transition for the "⬆ LEVEL UP!" line. No pay-to-win: nothing here gates gameplay.
- **As-built v1.21 monetization-ready shop: cosmetics only by schema (round 47, D31):** data-driven `ShopItemData` catalog + earn rates in `content/shop/shop.tres` (8 items, 3 price tiers); the cosmetics-only rule is schema-enforced - an item's entire effect is a (hero, variant) cosmetic unlock, so a gameplay item cannot be represented in content; `validate` checks resolution, pricing, known currencies, the no-free-default rule and unique pairs. Currency "gear" is earned in matches (`apply_match` -> `ShopBank.gear_for`, data-driven per-match + win bonus); the ledger (`profile.currency`) and the purchase engine (`profile.buy_item`, one-shot, debits exactly, grants the variant) are currency-agnostic - a payment provider can add currencies later without touching the flow. Purchased variants grant cosmetic early access like achievements/events: `HeroVariantBank.color_for` caps on max(mastery, every grant), and the new `HeroVariantBank.variant_unlocked` helper (mastery gate OR any grant via `profile.grant_sources()`) now drives the hero-select variant dots - which also fixed D26/D29 grants looking locked in the dots pre-D31. Save compat: pre-D31 saves load with empty shop state; a latent bug this round - JSON round-trips int grants as floats and `Array.has(int)` fails on `[1.0]` - is fixed by normalizing all grant lists to ints at `PlayerProfile.load` (fixes the dots, buy_item dedup and color_for after any reload). Shop UI: hero-select SHOP button + panel (gear balance, catalog rows, BUY/OWNED) in `ui/hero_select.gd`; the shop is local/cosmetic like the rest of progression (no wire changes). test_shop (13 checks) covers bank + validate catches, data-driven earning, short-balance block, exact debit, one-shot, mastery-1 selectability with the control clamp, variant_unlocked, old-save compat + round-trip, grant_sources, and the UI rows.
- **As-built v1.20 events framework: participation/win/objective rewards (round 46, D29):** cosmetic-only limited-time events - `EventData` resources in `content/events/events.tres` (3 shipped: Forge Week = 10 matches, Capture Push = 5 Capture wins, Objective Sprint = 3 MVPs on objective modes; each grants one hero's cosmetic variant), evaluated at match end by `EventBank.events_completed` from `PlayerProfile.apply_match` (new optional `mode_id` param, old call sites default "" so mode-filtered events simply do not count them). An event = goal kind (MATCHES/WINS/MVP) + optional mode filter + target + cosmetic reward; one-shot via `profile.event_done`; progress in `profile.event_progress`; grants in `profile.event_variant_unlocks` (the variant bank's effective cap is now the max of the mastery gate, the achievement grants AND the event grants - cosmetic-only by construction, mirroring D26). The no-hero-forcing rule: an event never requires/filters/rewards a specific hero (the reward hero is only whose variant is granted). `EventBank.load_bank` copies the event array (the cached resource's EventData entries are shared and treated read-only by the engine - a test that mutates a loaded entry leaks into the ResourceLoader cache). The results overlay renders "EVENTS COMPLETE" rows via `EventBank.view_rows` (both net and local paths feed `info["events"]`). No wire changes - events are local/cosmetic like the rest of progression. test_events (15 checks) covers bank integrity + validate() catches, exact participation threshold, single + multi-mode filters, inactive events, one-shot, reward gating at mastery 1 with the control clamp, cosmetic-only mastery, old 7-arg call sites, old-save compat + round-trip, and the view rows. Same round, **D30 reconnect reliability** (root-caused from a persistent net_profiles flake; bisected to fail on pre-D29 code too): a slot reply lost on a lossy link dead-ended the connect (packets drop, the client never re-sent its hello). Fix: (1) the client gains a bounded re-hello watchdog (`REHELLO_TIMEOUT` 2 s, `MAX_REHELLOS` 4) - while `my_id < 0` it re-sends the hello with the same token; (2) the server treats a hello from a peer that ALREADY holds a slot as an idempotent re-hello and re-announces that slot (`_send_slot`) instead of fresh-joining a second, orphaning character (the client sends exactly one hello per transport connection, so this is unambiguous). test_net_sim + test_net_profiles now KILL the reply path (100% loss) around the token reattach: the slot can only confirm via the watchdog + re-announce, and both suites assert the SAME character comes back with no extra character spawned. Both went from flaky (2/3 and 3/3 red across the round) to green and deterministic.
- **As-built v1.19 matchmaking v2: win-probability queue (round 45, D28):** the 4-stage queue's SKILL stage is now a real fairness gate (it was a same-region duplicate of STRICT in v1). New data-driven `MMConfig` resource (`content/matchmaking.tres`; `core/matchmaking/mm_config.gd`): stage windows (strict 5 s / skill 15 s / region 60 s), the fairness band (`max_team_delta` 250), the neutral bot rating (`bot_skill` 1000) and the logistic win-probability steepness (`skill_k` 0.004) - the lobby server loads it in `setup()` (fields stay public; tests override the windows). The lobby keeps a per-match SKILL LEDGER (`skill_sum`/`ledger_humans` on the match entry): assignments add the party's ratings (skill > 0 only), and T_STATE human-count drops decay the ledger proportionally (documented approximation for unknown departures). `MMConfig.team_projection` projects a join even-split (the lobby does not see team assignment - the match server does - with bot seats at `bot_skill`) and `fair_join` gates it: |projected avg team delta| <= band. A party is projected as a block (size x average rating) - grouping-advantage handling: stacked groups are rejected in the SKILL stage, and unknown ratings (v1 clients send 0) stay neutral (exactly the v1 behavior). The REGION stage sort prefers fair matches inside the (LATAM-priority) widen order; BOTFILL keeps the v1 emptiest-first rule. `T_ASSIGN` now carries `win_prob` (logistic chance for the joining side, -1 when unknown) and the queue UI shows it. The v1 vote rules, stage ladder, and wire protocol are otherwise unchanged (T_JOIN's `skill` field was already part of the protocol). test_matchmaking_v2 (14 checks) covers config load, win_prob math, projection/fairness, grouping, live strict/v1 assignments with win_prob, the SKILL gate, the REGION fair-preference, BOTFILL ordering, ledger decay, and no-stranding.
- **As-built v1.18 map-vote anti-repetition (round 44, D27):** the lobby's map-vote domain now excludes the entry's current map from the NEXT-match vote pool - the map just played (or about to play, after a sticky decision) cannot be re-voted while alternatives remain (`MapRegistry.rotation_pool(exclude)` = registry ids minus the excluded map, falling back to the full list for the degenerate single-map case). The gate is server-side in `LobbyServer._on_vote` (kind "map"): a repeat vote is rejected with `err "repeat map <id> (current)"` before the weighted tally, so solo, party-member (non-leader) and party-leader votes all honor it. Every `mapvoteresult` response now carries `repeat` (the entry's current map id) so clients can gray the option out (main.gd results screen: the current map's button is disabled with a "(last)" tag; the status line names it); the protocol is JSON-dict based, so the added key is backward-compatible (old clients ignore it). The v1 vote rules (last-write-wins, weighted parties, strict majority, >=2 entities, sticky decided) are unchanged. test_mapvote grew to 17 checks (repeat rejected pre- and post-decision, older map becomes votable again = rotation, party-leader repeat rejected, rotation_pool helper) and test_partyvote's map-domain scenarios moved off the default map to exercise the new rule without changing what they assert (20 checks).
- **As-built v1.17 progression v2: achievements + seasonal cosmetics (round 43, D26):** cosmetic-only progression grows two data-driven surfaces - (1) ACHIEVEMENTS: `AchievementData` resources in `content/achievements/achievements.tres` (10 achievements; conditions over profile counters; rewards = none or an early cosmetic-variant unlock), evaluated one-shot at match end by `AchievementBank.newly_unlocked` after `PlayerProfile.apply_match` updated the counters; server-side `CharacterEntity` gains `headshots`/`streak`/`best_streak` stats and `World.stats_rows` carries them (append-only columns 4-5); achievement variant unlocks make the effective variant cap the max of the mastery gate and the granted variants (`HeroVariantBank.color_for`). (2) SEASONAL COSMETICS: `SeasonData`/`SeasonBank` (`content/cosmetics/seasons.tres`) - a season is a named cosmetic pack of (hero, variant) entries plus a current-season pointer; cosmetic-only by construction (no stat fields, unlock rules unchanged). The results overlay (main.gd `_show_results`) now renders the new-achievement rows (pure text built by `AchievementBank.view_rows`) and the current season label. No match-affecting change; test_progression_v2 (23 checks) covers conditions, thresholds, one-shot grants, old-save compat, reward gating, stat columns, and season validation.
- **As-built v1.16 map pool to four (round 42):** Phase 7 item 3 - the D18 data-driven map system absorbed two more original layouts with zero code beyond the registry id list: `content/maps/sawmill.tres` (48 m, raised central ridge + two flanking wall lanes) and `content/maps/saltline.tres` (56 m, open field, two elevated corners, 4 ramps, potions on the platforms). No architectural change: `Map` resources + `MapRegistry` as designed; test_map grew to 17 checks (integrity + bounds validation for the whole pool, mode-follow on the new map, 6-bot fight on the 56 m map).
- **As-built v1.8 data-driven maps + mode/map picker (round 32, D18):** maps are now DATA — `Map` Resource (`gameplay/maps/map.gd`) with every layout value as @exports (size, spawn rows, cover boxes, crates, ramps, potions); the two maps ship as `content/maps/crossdocks.tres` (the Phase 1 placeholder geometry, renamed) and `content/maps/foundry.tres` (52 m, center core ring, ±18 spawns). `MapRegistry.get_map(id)` (unknown → default) + `Arena.build(world, map)` build from the data (empty map = legacy fallback). `MatchConfig.map_id` is written by the new hero-select MODE/MAP picker row (named `_pick_mode`/`_pick_map` handlers, headless-testable) and read by both hosts; M_SLOT gained a map_code byte so the client's MIRROR arena matches the server's map over the wire (live-verified: client probe built the foundry mirror from the slot). The lobby's reg message carries the match's mode and assign forwards it to queued clients (M_SLOT mode_code stays authoritative). Bot ladder: objective pressure — a CTF flag free for > 8 s pulls the lowest-instance-id teammate off their fight (new `World.flag_free_time` clock in CaptureMode.step; a fresh pick is not returned in the same step it was taken). Suite: test_map 12 checks; capture suite now 17.
- **As-built v1.7 Capture + Escort (round 31, D17):** the D16 framework's second wave — two more Mode resources on the same step/check_over/score_of/setup hooks. Capture (CTF): flags live at each team's central spawn (bases), steal radius 1.5 m, the carrier dies -> the flag drops (no decay), the flag's own team can re-secure a drop, an enemy flag carried home scores and returns the flag, 2 captures to win, timeout tiebreak = enemy-flag holder else draw; state on World (`flag_bases/flags/flag_carrier/captures`), new `Mode.on_kill` hook wired in `World.kill` for the drop. Escort: a payload rides the central lane between the spawn-side x's (team 0 pushes, team 1 brakes; power = clamped head-count, no reverse), delivery wins for team 0, timeout wins for team 1; state on World (`payload_*`). Wire: 4 `ext` u8 bytes in the snapshot (carrier codes for Capture, progress/speed for Escort, zeros otherwise) + mode_code 2/3 in M_SLOT; client visuals (flag boxes follow the carrier's view, base markers, payload mesh) + HUD labels ("RETURN THE FLAG!" / "OUR FLAG IS DOWN!" / "PAYLOAD n%"). Bot ladder: the CAPTURE intent now dispatches per mode (flag logic with a designated home-bot when the own flag is down; both teams orbit the payload, attackers ahead / defenders behind). 13 + 11 check sim suites; live-verified over ENet (`--mode=capture` full steal->capture->return->re-steal loop, `--mode=escort` bot delivery in ~26 s).
- **As-built v1.6 mode framework + Control (round 30, D16):** match rules are now data resources — `gameplay/modes/mode.gd` (`Mode extends Resource`: mode_id, display_name, `step(world, dt)`, `check_over(world)`, `score_of(world)`), with `TDMMode` (the legacy World rules, unchanged) and `ControlMode` (one central point, solo 15 s capture, contested freezes, captured point held until re-captured, 2 captures to win, timeout → captures → holder → draw). `World` owns all match state (`mode`, `control_active/point/owner/progress/progress_team/score`) and calls the two hooks in `step()`; `World.finish_match()` is the single over-path (the over-event score is the MODE's score, so TDM shows kills, Control shows captures). Hosts instantiate via `ModeRegistry.get_mode(MatchConfig.mode_id)`; the wire carries 3 control bytes in the snapshot (owner/team/progress u8) + a `mode_code` u8 in M_SLOT, and the bot ladder gained a CAPTURE intent (objective > curiosity, deterministic per-bot spot in the circle). 15-check sim suite; the legacy `mode == null` TDM path is untouched, so every pre-framework suite still runs unmodified.
- **As-built v1.4 match lifecycle (round 28, detail in NETWORKING.md §3, D14):** the dedicated server now plays match after match without a restart: `World.reset()` re-arms time/score/over on the same world+arena, `MatchServer.reset_match()` (triggered by a fresh hello into an over match with empty `slots`) frees every body, invalidates frozen tokens, and re-arms the world; the scene re-fills bots through `match_reset`. A live bot match ending at t=55.9 was reset in place by its first post-over joiner (slot + 19.9 Hz snapshot stream, discovery OVER->OPEN, one process, same ports). Verification: test_match_lifecycle (8/8, sim) + the live ENet run.
- **As-built v1.3 server ops (round 27, detail in server/Dockerfile + PERFORMANCE.md, D13):** the same headless server scene ships as a docker image — pinned engine binary from the official 4.7.2 release archive, project copied with its import cache, `CMD` = `res://net/server.tscn -- --port=7777`; lobby registration is a passthrough arg (`-- --lobby=... --lip=...`). A 2-core-capped container ran full 3v3 bot matches at ~0.19 cores avg with the 60 Hz step exact and 19.9 Hz snapshot pacing to a live client — the Phase 5 "2-core budget" item is measured, not estimated.
- **As-built v1.2 lobby (round 13, detail in NETWORKING.md §9, D12):** separate headless **UDP** sidecar process (lobby.tscn) — Godot 4.7 headless TCP creates no system socket (probed), so the lobby is UDP JSON-lines with application-level reliability (seq/dedupe/retransmit/rejoin); game servers self-register with a **published** address (--lip) and report live humans/over; clients queue through a **4-stage party-aware strategy** (strict → skill(no-op v1) → region widen with LATAM-priority table → botfill after 60 s); the lobby assigns the published server address and the client joins it directly (NAT traversal/relay = follow-up); hero-select ONLINE panel shows region + live ping + queue state. Server authority untouched — the lobby only routes joiners.
- **Regions**: initial SA/NA/EU/AS with LATAM priority (directive §18); region table now ships as core/matchmaking/regions.gd (6 regions, widen order LATAM-first); server ops latency table + client per-server ping still open; the client shows live lobby ping today.

### 3.11 Offline & Community (directive §19/§20)

- **Offline = same client + local headless server in-process** (one binary, "local match" mode): training, bot matches, tutorials, challenges, practice range — zero cloud dependency.
- **LAN**: local server discovery + direct connect (Phase 5 first, per directive "start with LAN testing").
- **Dedicated/community servers** (Phase 8): docker image (server/Dockerfile), one container per match, simple API (create/join/list), mod folder mount — the community can run the game forever.

### 3.12 Content Pipeline & Modding (directive §31)

- Every content item (hero, weapon, ability, perk, map, mode, balance block, cosmetic) is a **Resource with a stable id + schema version**.
- content/ is the shipped set; mods/ overlays by id (override) or adds new ids. Validation tool (headless) checks schema, references, and balance bounds before a mod loads.
- Core API surface for mods is versioned and documented (CONTRIBUTING.md); the engine (Godot) stays MIT and moddable at the GDExtension level.

### 3.13 Performance Architecture (budgets in PERFORMANCE.md)

- Object pooling: projectiles, VFX, pickups, damage numbers (no runtime allocs in the sim).
- LOD + occlusion culling on maps; baked lighting; texture compression (ETC2/ASTC for Android); resolution scaling + quality presets (low 30 / mid 60 / high 60–120).
- Server budget: 1 match (up to 6v6 + bots) on a 2-core container at 60 Hz (measured in Phase 5).
- Rendering: batched static geometry per map, GPU particles with pool caps, scalable shadows (off/low on low-end).
- **Measure on real Android hardware** (directive §25) from the Phase 1 vertical slice onward — never desktop-only.

## 4. Key Decisions Log (Phase 0)

| # | Decision | Rationale | Alternatives considered |
|---|---|---|---|
| D1 | Godot 4.7.x, GDScript-first | Open-source mission, mobile fit, ecosystem match, agent-friendliness | Unity (proprietary engine — fails §32) |
| D2 | Authoritative headless server, same codebase | Anti-cheat, community servers, bot parity, reconnect | P2P (fragile, hard to host); separate server language (duplication) |
| D3 | Bots are server-side controllers | Feature, not filler; parity with humans; testable headless | Client-side bots (desync, cheatable); separate AI process (latency, complexity) |
| D4 | 3v3 tuned default; team size = config | T3 lesson: 5v5 broke identity; OW 6v6 experiments confirm small-team pressure | Hard-coded 3v3 (loses §3 flexibility) |
| D5 | Data-driven heroes/weapons/abilities/perks/modes | §8/§9/§31 extensibility; headless balance testing; modding | Per-hero scripts (OW-style, but less moddable) |
| D6 | In-match perks: 2 levels × 2 choices (OW model) | Fits 3–8 min matches; proven by OW S15; choice > numbers | 3+ levels (too long); no perks (loses §8) |
| D7 | Regen-to-cap HP + map pickups (T3 pattern) | Mobile-friendly sustain; objective-adjacent pickups drive positioning | Full regen (boring positioning); no regen (high-death frustration) |
| D8 | Combat-driven ult charge (T3/OW) | Rewards play, no idle charging, consistent across modes | Time-based (T3's mode-specific ult complaint) |
| D9 | One map per phase-pair, 2 maps at Phase 6 | Compact, readable, mobile-scale | Big maps (T3's maps were right-sized; OW-scale too big for 3v3 mobile) |
| D10 | Local-first accounts, optional cloud | Offline-first mandate; no cloud dependency for core play | Account-gated (loses §19) |
| D11 | Net v1 (round 9): ENet via Godot 4.7 SceneMultiplayer; 60 Hz server, 20 Hz snapshots (unreliable) + reliable events, 100 ms interpolation. **v1.1 (round 10) refresh:** + input sanitization/seq gate, session-token reconnect (same-character reattach), lag-comp hitscan (200 ms analytic rewind, single world.hitscan truth), client prediction via a twin driven by the real NetPlayerController (hard-snap > 0.35 m), SimLink net-sim transport (150 ms RTT + 2% loss suite) | v1 shipped the authoritative loop + LAN connect first (LAN-first mandate §21); v1.1 adds feel + robustness behind the same authority rules — prediction runs the sim through the same controller interface (D3), lag-comp only bends validation (never damage), reconnect reuses the server character (no state resync needed) | Prediction from day one (feel now, complexity now); 30 Hz snapshots (bandwidth for feel); full physics re-sim for lag-comp |
| D12 | **Lobby prototype (round 13)**: separate headless **UDP** sidecar (JSON-lines, seq/dedupe/retransmit every 2.5 s, fresh-seq rejoin after 4 s silence — Godot 4.7 headless TCP creates no system socket: probed, listen() OK but no fd/ss entry/cross-process connect error); 4-stage party-aware queue (strict → skill no-op v1 → region widen LATAM-first → botfill @ 60 s, fewest-humans order); **published-server addressing** (game server self-registers with --lip; lobby assigns that address to joiners); peers and matches reaped on 5 s silence | LAN-first §21: internet v1 with zero new infra beyond one headless process; UDP is the only real socket in 4.7 headless; published addressing leaves the game server unchanged except registration + state broadcast and needs no relay; party v1 = room >= party (handshake deferred) | TCP lobby (dead in 4.7 headless); in-process lobby (couples match lifetime to the lobby); relay/hole-punch from day one (separate follow-up); full skill/MMR ranking in v1 (no match data yet) |
| D13 | **Server ops v1 (round 27)**: dedicated-server **docker image** (`server/Dockerfile`: debian:bookworm-slim + pinned Godot 4.7.2 release binary + `game/` project + import cache baked at build); one container per match; ENet 7777/udp + discovery 7778/udp exposed; `--cpuset-cpus` 2-core profile verified at ~0.19 cores avg with the 60 Hz step held (world-time ratio 1.0000) and 20 Hz snapshot pacing through published ports (PERFORMANCE.md) | The headless server IS the product for LAN/hosted play (directive §22); docker gives community servers a one-command start; the budget proves a 2-core VPS hosts 3v3 with >90% headroom | Bundling the Godot binary in-repo (license/distribution friction; pinned release URL instead); systemd-only hosting (no portable image); `--network host` as the default (port clashes on shared hosts; bridge + published ports is the default, host net is the documented alternative) |
| D14 | **Match lifecycle v1 (round 28)**: a finished match is reusable — a fresh join with **no humans connected** starts a new match **in place** (`World.reset()` + `MatchServer.reset_match()`: fresh score/time/over, all bodies freed, frozen tokens invalidated, bots re-filled by the scene via `match_reset`); joins while other humans are still connected are rejected with the over code (no mid-observation reset in v1). Sim 8-check suite + live ENet verification (OVER -> join -> OPEN, same process/ports) | The server image is a 2-core, one-process host: restarting the container per match is an ops cost (image pull/startup, published-addr churn in the lobby, discovery blips) that the directive's "3-8 min matches, bot-filled" cadence makes frequent; in-place reset reuses the process, the ENet ports, the lobby registration and the discovery socket — zero new wire messages because v1 only resets when nobody is connected | Container restart per match (simplest, but lobby churn + startup latency per match); full mid-match reset with observing clients (needs a new wire message + client-side final-dismissal, deferred); slot locks across reset (the joiner-triggered rule is simpler and matches the "server dies with the last player" expectation) |
| D15 | **NAT traversal v1: relay (round 29)**: a standalone relay (res://net/relay.tscn) accepts server registrations over **outbound UDP** (R_REG [token, game_port]) and grants each match a **virtual port** (7901..8156, R_OK); clients connect to `<relay_ip>:<vport>` with the **unchanged** ENet protocol and the relay pumps raw datagrams per (match, client) — one ephemeral socket per link (distinct source ports to the server; replies return through the vport socket so the dialed address holds); R_PING keep-alive 10 s; eviction 60 s match / 120 s link; lobby registration deferred until the vport exists and advertises it (server `--relay=ip:port`). Verified: 7-check suite + live docker-bridge NAT test + full lobby→assign→join (NETWORKING §9.5) | A NAT mapping only exists for the server's OUTBOUND traffic; a dumb headerless pump over those mappings needs no client changes, no STUN server, and keeps ENet's reliability/flow control intact (the relay parses nothing but two control datagrams); vport-per-match bounds state (256 matches ≫ the 2-core budget) and makes the published address self-describing | Direct hole-punching for every NAT pair (symmetric NATs need a fallback anyway; punch + relay-fallback is the follow-up); a per-packet relay protocol (re-implements ENet reliability the stream already has); relaying the lobby too (the lobby is a client-facing service that can be hosted openly; only the NAT'd match needs the pump); auth token in v1 (R_REG token field reserved, = 0) |
| D16 | **Mode framework v1 + Control (round 30)**: match rules as data resources (`Mode extends Resource` with `step`/`check_over`/`score_of` hooks; `TDMMode` mirrors the built-in TDM, `ControlMode` implements the central-point capture rules) chosen per match by `MatchConfig.mode_id` via `ModeRegistry`; all objective STATE lives on `World` (reset-safe with D14's in-place reset), all objective LOGIC in the Mode; the snapshot gains owner/team/progress bytes, M_SLOT a mode_code, and bots get a CAPTURE intent so offline squads take the point. v1 scope: one fixed central point, no decay/neutral-reset, no rotating points | The directive's Phase 6 needs multiple modes without forking the match loop; a Resource per mode keeps the server/authoritative path, the reset lifecycle, the wire format and the bot ladder mode-generic, and keeps the legacy TDM path intact (`mode == null`) so pre-framework suites are unmodified | Capture + Escort are the next two Mode resources from this same framework; the mode picker (hero-select) + map voting still need to write `mode_id` (v1 is config-driven); multi-point/rotating control and progress decay are v2 |
| D17 | **Phase 6 modes: Capture (CTF) + Escort (round 31)**: two more Mode resources from the D16 framework — the same four hooks (`setup`/`step`/`check_over`/`score_of`, plus the new `on_kill` hook for per-character objective state) make both modes drop into the server loop, the D14 in-place reset, the wire format and the bot ladder with no host changes. Capture: base-at-spawn CTF (steal radius 1.5 m, drop-on-death, own-team re-secure, 2 captures to win, timeout tiebreak = enemy-flag holder). Escort: 1-D lane payload between the spawn sides (head-count power, no reverse; delivery = attacker win, timeout = defender win). Wire: 4 `ext` u8 snapshot bytes + mode_code 2/3 in M_SLOT | The directive's Phase 6 needs four playable modes; the framework's payoff is that each new mode is a self-contained Resource + a handful of World state fields + 4 wire bytes instead of a fork of the match loop | CTF v1: no flag decay/damage, no mid-carry re-steal; Escort v1: no reverse, no lateral steering, head-count clamp; bot-only escort can end early (attrition) — human pacing is the acceptance criterion; Capture/Escort bot tuning (objective-vs-fight aggression) is Phase 7 territory |
| D18 | **Phase 6 maps + picker (round 32)**: maps as data (Map Resource + content/maps/*.tres; two original maps: Crossdocks 44 m, The Foundry 52 m center-core) + hero-select MODE/MAP picker writing MatchConfig + M_SLOT map_code for the client mirror arena + lobby reg/assign mode field + CTF objective-pressure bot rule (8 s stale flag pulls the designated teammate) | The directive's Phase 6 needs the four modes AVAILABLE from the UI and two original maps; making the arena a data resource is what makes map 3+ a .tres file instead of a code change, and the picker is the smallest path to "player chooses the match" | v1: no map VOTING UX yet (host's picker + lobby broadcast; in-lobby voting = follow-up); the Foundry's escort lane passes through the center core (v1 payloads ignore obstacles, D17); the picker writes the HOST's MatchConfig — remote-server operators use --mode=/--map= |
| D19 | **Phase 6 results + progression (round 33)**: server-side per-character stats (kills/deaths/damage in World.kill/apply_damage) + stats_rows()/mvp_index() shared by server and client + M_STATS wire (before match_over) + results screen (mode/map label, K/D/DMG table, MVP, XP line) + local cosmetic progression (ProgressionConfig content data + PlayerProfile user:// JSON, level curve, per-hero record) + hero-select badge | Every match must END somewhere meaningful, and the directive's progression point (levels/mastery, no P2W) needs a home; stats live server-side because score/kill accounting is sacred authority, and the char-index convention of the snapshot makes the wire trivially indexable | v1: XP rewards are win/kill/MVP only (no objective-contribution tracking, no damage-scaling XP); progression is LOCAL (no account/sync yet — the account system is a later phase); cosmetics are the next step on this rail |
| D20 | **Phase 6 next-match mode voting (round 34)**: lobby protocol v1.3 (vote/voteresult/setmode) + per-match tally with one-vote-per-peer last-write-wins + strict-majority decision (>= 2 votes, sticky) + directory mode flip + assign vote state + MatchServer pending-mode applied at reset_match (pure-data mode swap, no live mid-fight changes) + results-screen vote UI (four mode buttons, live tally line) | The directive's phase-6 "party-aware" matchmaking needs a way for the community to steer the host's default mode without a new authority; the lobby already knows both the voters and the match, so it is the natural venue; applying at reset keeps server authority sacred (the server validates and only swaps between matches) | v1: mode-only voting (map voting, pre-start in-match application, per-party bundles are v2); votes are community pressure, not binding on the host's first match; no vote UI in-match (results screen only) |
| D21 | **Phase 6 next-match map voting (round 35)**: domain-generic lobby voting (mode + map independent tallies, protocol v1.4 mapvote/mapvoteresult/setmap) + reg/state/assign map fields + scene-level arena rebuild at the next in-place reset (old arena freed, Arena.build, spawn points change; M_SLOT map_code already handles client mirror) + results-screen map vote row | Mode voting (D20) proved the lobby vote loop; maps were the other half of the phase-6 "steer the host's defaults" story, and reusing the exact same machinery (tally/decide/forward/apply-at-reset) keeps server authority sacred with minimal new surface | v1: two maps (crossdocks/foundry) - more maps arrive in phase 7; per-party vote bundles deferred (party handshake itself is v2); map voting applies between matches only (no live mid-fight swap, same as mode) |
| D22 | **Phase 6 cosmetics + mastery display (round 36)**: per-hero mastery as a pure function of the per_hero stat seed (data weights, shared level curve, old-save backward compat) + cosmetic variant palettes as content data (5 colors/hero, mastery unlock schedule [0,2,4,6,8], palette[0] pinned to the identity color) + profile-persisted selection with locked-pick clamping + hero-select mastery line / tappable variant dots / variant-colored card preview + the player character wears the selected variant in offline, practice and net play (own predicted character only) | Progression v1 (D19) already proved the local-cosmetic rail; mastery + variants give the rail visible, per-hero payoff without touching the wire or gameplay (no P2W) - and making mastery a pure function of the seed removes a whole drift-failure class | v1: 5 palettes x 6 heroes (tints only - no mesh/material swap, no trail/ult-skin differentiation), opponents' variants not broadcast (client tints own character only), no rarity/drop system - all variants earned by mastery |
 | D23 | **Phase 6 per-party vote bundles (round 37)**: lobby protocol v1.5 weighted tallies - the party leader's vote carries the declared party size (clamped 1..6), members are acked with party_vote=true and add no weight (the party speaks through its leader); decision = strict weighted majority (winner*2 > total) AND >= 2 voting entities, so a lone party cannot self-decide and equal parties tie; solo (no party_id) votes keep weight 1 and all-solo lobbies behave exactly like v1.4 (backward compatible both directions); party context rides the vote message because assigned peers leave the waiter queue (test_partyvote, 20 checks). |
 | D24 | **Phase 6 mobile controls customization (round 38)**: layout as data (ControlLayout baseline Resource in content/settings = the Phase 1 layout, button positions as viewport fractions) x user ControlSettings (aim sensitivity, button scale/opacity, joystick scale, fire-side mirror - clamped, persisted in the profile JSON, old saves = stock); ControlSettings.effective() is the pure resolver the TouchControls builds from and the desktop mouse path reads (LOOK_SPEED x aim sens, static data so gameplay/ stays UI-free); hero-select CONTROLS panel (steppers + toggle + reset) with immediate persistence; no wire changes (aim delta already client-scaled), no P2W (test_controls, 24 checks). |

## 5. Test Strategy (overview — directive §41)

- **Gameplay**: headless sim tests — movement, shooting (hitscan + projectile), damage pipeline modifiers, cooldowns, death/respawn, perk application, mode win conditions. (Godot built-in test framework or GUT; CI on PRs.)
- **Networking**: deterministic net-sim harness — latency/packet-loss injection, prediction reconciliation error bounds, lag-comp window correctness, reconnect flow, server-authority property tests (client lies are rejected).
- **Mobile**: touch input mapping tests (synthesized touches), resolution matrix (≥5 aspect ratios), FPS/memory/thermal profiles on a real device checklist per PERFORMANCE.md.
- **Bots**: headless bot eval suite — navigation success, objective time vs difficulty, K/D bands, team-formation checks; regression in CI.
- **Server ops**: dedicated-server smoke (docker up, join, play, disconnect, reconnect) in CI.

## 6. Open Questions (to resolve in Phases 1–5)

1. Exact Godot 4.7 vs 4.8 trade-offs at vertical-slice time (pin after first device test).
2. ENet vs WebRTC for the primary transport (ENet first; WebRTC if NAT traversal pain appears).
3. Bot AI: pure behavior tree vs utility-first with BT fallback (start utility scoring + BT; measure).
4. Whether to ship C# from the start for the net codec (benchmark GDScript codec cost in Phase 1).
5. Anti-cheat posture: server-authority + input rate checks first; device attestation later if needed (mobile reality).
6. Voice chat: third-party (EOS Godot, MIT) vs deferred to Phase 7 (start deferred; party text is P1).
